{
  "name": "blaze",
  "version": "1.0.0",
  "description": "Basic webpage for embedding blaze games",
  "main": "index.js",
  "scripts": {
    "start": "nodemon lib/index.js --exec babel-node --presets env",
    "build": "babel lib -d dist --presets env --minified --no-comments",
    "serve": "node dist/index.js"
  },
  "author": "Dreaded_X",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^1.18.3"
  }
}
